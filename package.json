{
  "name": "jssmp",
  "private": true,
  "description": "JS Scripts for SMP",
  "devDependencies": {
    "esbuild": "^0.14.14",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --outfile=dist/catrox_main.js"
  }
}
